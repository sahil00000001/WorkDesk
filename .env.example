# Application
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# External API (hosted backend)
# All API calls go to: https://zoho-backend-rho.vercel.app
# No local API routes are used — the backend is fully external.
